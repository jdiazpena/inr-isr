# Versions used for the behavior-equivalence verification in July 2026.
# Install a CUDA-compatible PyTorch build appropriate for the local GPU when needed.
numpy==1.26.4
pandas==2.2.3
matplotlib==3.8.4
h5py==3.15.1
tqdm==4.67.3
torch==2.11.0
